# Implementation Plan — M1

> Live task list for the build loop. The loop reads this, works the top unchecked task, ticks it off, appends to `loop/PROGRESS.md`.
> This plan is **DISPOSABLE** — regenerate from `docs/DESIGN.md` via planning mode if it goes stale.
> Acceptance: `loop/ACCEPTANCE.md`. Design: `docs/DESIGN.md` section {{N}}.

## Phase 0 — toolchain and gate (if not done)

- [ ] {{Scaffold project structure per DESIGN}}
- [ ] Wire `loop/scripts/gate.sh` and verify green on minimal scaffold
- [ ] Document commands in `docs/TOOLCHAIN.md`

## Phase 1 — {{name}}

- [ ] {{Task — test-first, one iteration: what to test, what to build}}

## Phase 2 — {{name}}

- [ ] {{Task}}

## Phase F — close out

- [ ] Reconcile `loop/PROGRESS.md` / `loop/HANDOFF.md`; verify all `loop/ACCEPTANCE.md` criteria; create `.loop/COMPLETE`

## Later (NOT this milestone)

- {{Explicit deferrals — prevents scope creep}}
