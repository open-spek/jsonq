// Phase 0 seed: a minimal export so every gate step (typecheck, lint, test,
// build) has real input. Replaced by the actual public surface -- query(),
// agg, and public types -- starting at loop/IMPLEMENTATION_PLAN.md task 3.1.
export const PACKAGE_NAME = "jsonq";

